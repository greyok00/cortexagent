# cortexagent — Design Principles Audit (2026-08-19)

> Read-only audit of existing surfaces against `docs/ux/DESIGN-PRINCIPLES.md` §5.
> Tkinter desktop console does not exist yet — covered as "would-block" report.

**Status legend** · ✅ pass · ⚠️ partial · ❌ fail · ➖ N/A (not in this surface)

---

## Critical gaps (must fix before next user-facing release)

| # | Gap | Surface | Evidence |
|---|-----|---------|----------|
| **G1** | No `aria-live` regions on any web surface — AI streaming output is invisible to NVDA/VoiceOver | Pipeline WebUI, Security WebUI | `lib/webui.py:209` serves `/pipeline` (pure 3D, no DOM); `assets/hardening-dashboard.html:79-181` JS rebuilds DOM without live regions |
| **G2** | No ARIA anywhere — no `role`, no `aria-label`, no semantic `<button>`/`<nav>`/`<main>` | All web | `grep -c "aria-" lib/webui.py lib/sec_webui.py` = 0/0; `assets/hardening-dashboard.html` uses raw `<div>`/`<ul>` only |
| **G3** | No visible focus indicator CSS — focus is invisible on keyboard navigation | Pipeline WebUI, Security WebUI | `assets/hardening-dashboard.html:6-48` — no `:focus`/`:focus-visible` rule; `pipeline-viz.html:8-17` — only one `#speed-slider` style |
| **G4** | Zero keyboard shortcuts in any web surface — `Cmd/Ctrl+K`, `Cmd/Ctrl+/`, `Esc` cancel, `Enter`/`Shift+Enter` composer all missing | Pipeline WebUI, Security WebUI | `grep -rn "Tab\|Enter\|Shift+Enter" lib/sec_webui.py` = 0; `assets/hardening-dashboard.html` has no `addEventListener` |
| **G5** | TUI status strip never checks `NO_COLOR` / `TERM=dumb` / non-TTY — always emits 24-bit ANSI | TUI | `lib/tui_status.py:61-78` hard-codes 24-bit `38;2;…` SGR; `grep "NO_COLOR\|--no-color" lib/tui_status.py` = 0 |
| **G6** | No OSC title updates with chat/model context — terminal title is stale | TUI | `grep -rn "OSC 0\|\\x1b]0" lib/` = 0 |
| **G7** | No skip-to-content link; no `<main>` landmark; no unique landmarks | Pipeline WebUI, Security WebUI | `assets/hardening-dashboard.html:50-77` — no skip link; only `<h1>`/`<h2>`/`<div>` |

---

## Defer until Tk build (will be HARD blockers for Tkinter desktop console)

| # | Item | Why it blocks | Current state |
|---|------|---------------|---------------|
| **T1** | `ttk` widgets, not raw Tk | §5.2 first item (HARD) — needed for system theme + fontconfig | Already done in `lib/overseer_dashboard/widgets.py:22` (`from tkinter import ttk`) |
| **T2** | `tkinter.font.nametofont("TkDefaultFont")` | §5.2 — must respect system font size | Not used in any Tk window (`grep "nametofont" lib/` = 0) |
| **T3** | Menu bar via `Menu` widget (macOS) | §5.2 — required on macOS | Not used |
| **T4** | Left treeview + center message list + bottom composer layout | §5.2 — mandated structure | No Tk console exists; `lib/sec_controls.py:439` uses single-column scrollable list only |
| **T5** | Tab order = visual order | §5.2 — HARD | `lib/overseer_dashboard/ui.py:236-255` actively **disables** `takefocus` on most widgets (anti-pattern) |
| **T6** | Composer `Enter` sends, `Shift+Enter` newline | §5.2 — matches web | No composer widget exists |
| **T7** | Focus ring on every interactive widget | §5.2 — HARD | `lib/overseer_dashboard/ui.py:253` `highlightthickness=0` strips focus ring globally |
| **T8** | `tkinter.filedialog` native dialog | §5.2 — HARD | Not used |
| **T9** | Settings as modal `Toplevel` | §5.2 — HARD | `lib/sec_controls.py:604` does use `tk.Toplevel(root)` for confirm dialog (positive precedent) |
| **T10** | Honest a11y flag in README | §5.2 — HARD (per DESIGN-PRINCIPLES §2.2) | `README.md:1-117` — zero mention of Tka11y, Orca, NVDA, screen-reader limits |

---

## §5.1 — TUI checklist

| Checklist item | Status | Evidence |
|----------------|:------:|----------|
| All actions reachable without mouse | ⚠️ | Renderer `lib/tui_status.py:1-675` is pure view-model (no input). Keyboard handling lives in the uncommitted local-only `lib/tui.py` (per memory `cortexagent-tui-response-model.md`) — not auditable |
| `Tab`/`Shift+Tab` cycle panels | ➖ | Renderer is presentational only; keybinding layer untracked |
| `?` opens help; shows every keybinding | ➖ | `lib/tui_status.py:267-269` lists shortcuts (`?`, `m`, `s`, `l`) in `StatusView.shortcuts` dataclass — but it's just a label, no handler |
| Slash command vocabulary matches cross-platform list | ➖ | Slash commands live in vendored `cortex/packages/coding-agent/src/cli/` (not cortexagent's own TUI) — out of audit scope |
| `Esc` cancels generation; second `Esc` clears input; third exits | ⚠️ | Renderer prints literal `"Esc cancel"` (`lib/tui_status.py:456, 489`) but no binding layer in cortexagent |
| `Ctrl+C` / `Ctrl+D` follow Claude Code two-stage semantics | ⚠️ | Mentioned in `lib/tui_status.py:515` (`ctrl+c`) as footer label only; actual binding lives in vendored cortex |
| Auto-scroll during stream; manual scroll pauses | ➖ | Not present in `lib/tui_status.py` (renderer only) |
| `G` / `End` jumps to live output | ➖ | Not in cortexagent code (lives in vendored cortex TUI) |
| Status line always visible (mode, model, streaming, permissions) | ✅ | `lib/tui_status.py:555-621` `strip_render` produces Runtime/SlimToken/Memory panels every tick |
| Footer key-bar visible | ✅ | `lib/tui_status.py:480-509` `footer_line` renders `(key, label)` shortcuts with shrink-to-fit |
| `--no-color`, `--plain`, `--unicode` flags | ❌ | `lib/tui_status.py` — no flag parser; assumes 24-bit color |
| Respects `NO_COLOR`, `TERM=dumb`, non-TTY | ❌ | `lib/tui_status.py:61-78` hard-codes 24-bit ANSI; no env checks (also see G5) |
| ANSI uses 8-color + bold/dim + glyph + label for every status | ✅ | `lib/tui_status.py:80-89` `_STATUS_GLYPH` pairs every state with glyph + color; `WorkPhase` enum (186-195) drives text labels |
| OSC title updates with current context | ❌ | Zero `OSC 0` or `\\x1b]0` in `lib/` (also see G6) |
| Long output doesn't break width (wcwidth) | ✅ | `lib/tui_status.py:52-53` imports `wcwidth.wcswidth`; `display_width()` and `fit_to_cells()` handle wide chars + ellipsis |
| Box-drawing ASCII by default; Unicode behind `--unicode` | ❌ | Always emits Unicode `╭╮╰╯│─` (`lib/tui_status.py:417-435`) — no `--unicode` flag, no ASCII fallback |
| Glyph + label pairs (severity) | ✅ | `lib/tui_status.py:80-89` glyph table; `work_line()` always pairs glyph + dim text (441-475) |

---

## §5.3 — Web checklist (Pipeline WebUI + Security WebUI)

### §5.3 — Pipeline WebUI (`lib/webui.py` + `pipeline-viz.html`)

| Checklist item | Status | Evidence |
|----------------|:------:|----------|
| axe-core in CI; no axe errors | ❌ | No CI config references axe; `grep -rln "axe" /home/grey/cortexagent/` = 0 |
| Interactive elements are real `<button>`/`<a>`/`<input>` or ARIA-correct | ✅ | `pipeline-viz.html:21` uses real `<input type="range">` |
| Visible focus indicator with ≥3:1 contrast | � | `pipeline-viz.html:8-17` has no `:focus` rule |
| Skip-to-content link | ❌ | No `<a href="#main">` skip link at top |
| Message list semantic `<ul>`; `aria-live="polite"` for streaming | ➖ | N/A — pure 3D viz, no chat surface |
| Throttle live-region announcements | ➖ | N/A — no live region |
| Every tool call announced | ➖ | N/A — no tool calls in viz |
| Composer `Enter` send / `Shift+Enter` newline | ➖ | N/A — no composer |
| Command palette `Cmd/Ctrl+K` | ➖ | N/A — viz is presentational |
| `Cmd/Ctrl+/` shortcut panel | ➖ | N/A |
| Modal dialogs trap focus; `Esc` closes | ➖ | N/A — no modal |
| Sidebar left; inspector right | ➖ | N/A |
| Touch targets ≥48×48 CSS px | ❌ | Speed slider has no min-height (`pipeline-viz.html:12-16`) |
| Color contrast 4.5:1 text / 3:1 UI | �️ | `#c9a84c` gold on `#0a0a0f` near-black: needs contrast check; `#8a7d68` muted text on `#0a0a0f` likely fails |
| Manual NVDA + VoiceOver pass | ❌ | No documented pass |
| Two disabled-user sessions/year | ❌ | None scheduled |

### §5.3 — Security WebUI (`lib/sec_webui.py` + `assets/hardening-dashboard.html`)

| Checklist item | Status | Evidence |
|----------------|:------:|----------|
| axe-core in CI; no axe errors | ❌ | No axe dep anywhere in repo |
| Real `<button>`/`<a>`/`<input>` | ⚠️ | `assets/hardening-dashboard.html:80-181` uses `<a>` for NVD link (line 125), but pills are `<span class="pill">` — presentational, not interactive (acceptable since they're labels, not buttons) |
| Visible focus indicator ≥3:1 | ❌ | `assets/hardening-dashboard.html:6-48` has no `:focus`/`:focus-visible` rule |
| Skip-to-content link | ❌ | No skip link at top of `<body>` |
| Message list semantic `<ul>`; `aria-live` | ⚠️ | `<ul>` is used (lines 123, 142) but no `role="log"` or `aria-live` attribute |
| Throttle live-region announcements | ❌ | No live region exists |
| Every tool call announced | ➖ | No tool calls surface here (SIEM/SOAR is read-only display) |
| Composer `Enter`/`Shift+Enter` | ➖ | N/A — read-only dashboard |
| Command palette `Cmd/Ctrl+K` | ❌ | None |
| `Cmd/Ctrl+/` shortcut panel | ❌ | None |
| Modal dialogs trap focus; `Esc` closes | ➖ | N/A — no modals |
| Sidebar left; inspector right | ➖ | N/A — single-column grid |
| Touch targets ≥48×48 CSS px | ⚠️ | `<a>` NVD links (line 125) are text-only with no padding; pills (line 29) have `padding: 2px 8px` — likely under 24px |
| Color contrast 4.5:1 text / 3:1 UI | ⚠️ | Tokens defined (`--ok: #4ade80`, `--fail: #f87171`, `--warn: #fbbf24`, `--unknown: #94a3b8`) on `#0a0e1a` bg — `#fbbf24` yellow on near-black likely passes; `#94a3b8` unknown on `#0a0e1a` borderline; `#f87171` red on near-black borderline; **pills put #000 text on these colors** — `#000` on `#f87171` ≈ 4.5:1 (likely passes), `#000` on `#4ade80` ≈ 9:1 (passes) |
| Manual NVDA + VoiceOver pass | ❌ | None |
| Two disabled-user sessions/year | ❌ | None |

---

## §5.2 — Tkinter desktop checklist (would-block report)

| Checklist item | Status | Evidence |
|----------------|:------:|----------|
| Use `ttk` widgets, not raw Tk | ⚠️ | Tk windows exist (`lib/sec_controls.py:439` `tk.Tk()`, `lib/sec_tray.py`, `lib/overseer_dashboard/ui.py:24-26`) but the dedicated **desktop console** (chat surface) does not exist |
| `tkinter.font.nametofont("TkDefaultFont")` | � | Not used anywhere — `grep "nametofont" lib/` = 0 |
| Menu bar via `Menu` widget (macOS) | ❌ | Not used; `lib/sec_tray.py` uses pystray, not Tk |
| Left treeview + center + bottom composer | � | Desktop console doesn't exist |
| Tab order = visual order | ❌ | `lib/overseer_dashboard/ui.py:236-255` aggressively disables `takefocus` on most widgets |
| All buttons have text labels or accessible tooltip | ⚠️ | `lib/sec_controls.py` uses text buttons; some Tk windows use emoji-only labels |
| Composer `Enter` sends, `Shift+Enter` newline | ➖ | No composer exists |
| Color palette maps to GNOME; 4.5:1 contrast | ⚠️ | `lib/sec_controls.py:67-91` defines custom palette, not GNOME; `#808090` FG_DIM on `#0f0f1f` BG_DEEP ≈ 4.0:1 — **fails 4.5:1** |
| Focus ring on every interactive widget | ❌ | `lib/overseer_dashboard/ui.py:253` strips `highlightthickness` globally |
| Window resize preserves layout (`grid` + `sticky`) | ⚠️ | `lib/overseer_dashboard/ui.py:50-51` window is **fixed-size, non-resizable** — anti-pattern for §5.2 |
| `tkinter.filedialog` native dialog | ❌ | Not used |
| Settings as modal `Toplevel` | ⚠️ | `lib/sec_controls.py:604` uses `tk.Toplevel` for confirm dialog — positive precedent |
| **Honest a11y flag in README** | ❌ | `README.md` (117 lines) makes zero mention of screen-reader limits, Tka11y, Orca, NVDA, or any a11y caveat |

---

## Per-surface summary

| Surface | Pass | Partial | Fail | N/A |
|---------|:----:|:-------:|:----:|:---:|
| TUI (`lib/tui_status.py` — renderer only) | 3 | 1 | 4 | 7 |
| Pipeline WebUI (`lib/webui.py` + `pipeline-viz.html`) | 1 | 1 | 4 | 9 |
| Security WebUI (`lib/sec_webui.py` + `assets/hardening-dashboard.html`) | 0 | 3 | 6 | 6 |
| Tkinter desktop (does not exist) | 0 | 2 | 7 | 4 |

**Net**: the renderer half of TUI is well done (glyph+label, wcwidth, status line, footer key-bar). Everything web/Tk fails HARD on accessibility — zero ARIA, zero focus styles, zero keyboard, zero axe, zero a11y honesty in README.
